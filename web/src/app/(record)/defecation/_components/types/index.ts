// NOTE(taehyeon): 배변 기록 폼 타입
export interface DefecationState {
  selectedWhen: Date;
  selectedTry: string;
  selectedColor: string;
  selectedShape: string;
  selectedPain: string;
  selectedTimeTaken: string;
  selectedOptional: string;
}
